import { ArticleCategory } from './article-category.entity';
import { Article } from './article.entity';
import { Editor } from './editor.entity';

const ENTITIES = [Editor, Article, ArticleCategory];

export default ENTITIES;
